(def estring-1 "Cannot keep point-form on constraint-surface.")

(def estring-2 "Required translation is inconsistent with coupled rotation.")

(def estring-3 "Required rotation is inconsistent with available DOF's.")

(def estring-4 "Dimensional over-constraint.")

(def estring-5 "Surfaces do not intersect in a point.")

(def estring-6 "Surfaces do not intersect in a 1D curve.")

(def estring-7 "Cannot put marker into position with available DOF's.")

(def estring-8 "Cannot put marker into alignment with available DOF's.")

(def estring-9 "Over-constrained marker in inconsistent location.")

(def estring-10 "Unexpected degeneracy in geometry.")
